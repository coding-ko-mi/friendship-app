# Скопируй этот файл в .env и подставь свои значения. .env в Git НЕ коммитим.

# Строка подключения к PostgreSQL (async-драйвер asyncpg).
# Формат: postgresql+asyncpg://ПОЛЬЗОВАТЕЛЬ:ПАРОЛЬ@ХОСТ:ПОРТ/ИМЯ_БД
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/friendship
