node_modules
dist
.env
*.local

# Инкрементальный кэш TypeScript-компилятора (--incremental).
# Это локальный артефакт сборки, ускоряет повторную компиляцию tsc, но
# в репозитории не нужен — пересоздаётся при следующем `tsc`/`vite build`.
*.tsbuildinfo
